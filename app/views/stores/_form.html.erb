<%= form_with(model: store, local: true) do |form| %>
  <% if store.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(store.errors.count, "error") %> prohibited this store from being saved:</h2>
      <ul>
        <% store.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :store_name %>
    <%= form.text_area :store_name %>
  </div>
  <div class="actions">
    <input type="submit" class="btn btn-secondary" name="commit" value="Create Store" data-disable-with="Create Store">  
    <%= link_to 'Voltar', stores_path, class:"btn btn-primary" %>
  </div>
<% end %>
